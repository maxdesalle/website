{{- /* Project override of the etch theme posts feed, updated for Hugo >= 0.144:
       .Site.Author was removed — read author/email from site params instead. */ -}}
{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}
{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
{{- $pages = $pctx.RegularPages -}}
{{- else -}}
{{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
{{- $authorName := site.Params.author -}}
{{- $authorEmail := site.Params.email -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq  .Title  .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{.}} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne  .Title  .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>{{ with site.Language.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with $authorEmail }}
    <managingEditor>{{.}}{{ with $authorName }} ({{.}}){{end}}</managingEditor>
    <webMaster>{{.}}{{ with $authorName }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{ with .OutputFormats.Get "RSS" }}
	{{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{ end }}
    {{- /* Home feed tracks everything: posts, podcast episodes, talks, piano,
           photography — every regular page, newest first. Articles carry full
           text; other types carry their description/summary. */ -}}
    {{ range where site.RegularPages.ByDate.Reverse "Draft" false }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>{{ if not .Date.IsZero }}
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>{{ end }}
      {{ with $authorEmail }}<author>{{.}}{{ with $authorName }} ({{.}}){{end}}</author>{{end}}
      <guid>{{ .Permalink }}</guid>
      <description>{{ if or (eq .Type "posts") (eq .Type "post") }}{{ .Content | html }}{{ else }}{{ or .Description .Summary | html }}{{ end }}</description>
    </item>
    {{ end }}
  </channel>
</rss>
